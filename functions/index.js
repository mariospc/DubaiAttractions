const {
    getAllAttractions,
    getAttraction,
    // updateAttraction    
} = require('./attractions');

const {
    signUp,
    logIn
} = require('./users');

module.exports = {
    getAllAttractions,
    getAttraction,
    // updateAttraction
    signUp,
    logIn
}